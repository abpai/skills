---
description: Trace a real code path into a dense .understand walkthrough and scratch skeleton.
argument-hint: "[symbol, feature, or file/module path]"
---

# /code:understand

Use the `understand` module.

1. Read `internal/understand.md`.
2. Resolve the requested symbol, feature, or file/module path.
3. Disambiguate before tracing if multiple entry points are viable.
4. Write the walkthrough to `.understand/<topic>.md`; do not render it in chat.

User input: $ARGUMENTS
