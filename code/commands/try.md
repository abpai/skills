---
description: Evaluate a new library, tool, package, or repo before adopting it.
argument-hint: "[library, package, or repo]"
---

# /code:try

Use the `try` module.

1. Read `internal/try.md`.
2. Set up an isolated evaluation workspace.
3. Prove primitives with runnable scripts and finish with an honest verdict.

User input: $ARGUMENTS
