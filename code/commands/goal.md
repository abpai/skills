---
description: Define or audit a coding goal so it is specific, bounded, and verifiable.
argument-hint: "[goal, ticket, task, plan, or empty to use conversation context]"
---

# /code:goal

Use the `goal` module.

1. Read `skills/code/goal.md`.
2. Score the request on verifier, scope, context, stop conditions, and output.
3. Ask one clarifying question only when the lowest-scoring dimension blocks readiness.
4. Return `READY`, `NOT READY`, or `HUMAN_DECISION` with the goal brief and scorecard.

User input: $ARGUMENTS
